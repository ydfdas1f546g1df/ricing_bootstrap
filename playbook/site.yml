---
- name: Ricing Arch System
  hosts: local
  become: true
  vars:
    ansible_user_id: "ansible"
    main_user_id: "arch"
  gather_facts: true
  roles:
    - base
    - yay
    - ssh
    - shell
    - firewall
    - loginmanager
    - hyprland
#    - devtools
#    - waybar
#    - dotfiles
#    - backup
